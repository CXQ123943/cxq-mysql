SELECT ENAME, SAL
FROM EMP
WHERE SAL BETWEEN 2450 AND 3000;

SELECT ENAME, SAL
FROM EMP
WHERE SAL >= 2450
  AND SAL <= 3000;

SELECT ENAME, SAL
FROM EMP
WHERE SAL NOT BETWEEN 2450 AND 3000;

SELECT ENAME, SAL
FROM EMP
WHERE SAL < 2450
  OR SAL > 3000;